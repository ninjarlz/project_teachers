import 'package:flutter/material.dart';
import 'global.dart';

class ThemeSearch {
  BoxDecoration searchContainer = BoxDecoration(
    color: ThemeGlobalColor().containerColor,
    borderRadius: BorderRadius.circular(5)
  );
}
